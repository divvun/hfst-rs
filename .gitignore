# Rust build output
/target

# Editor / OS cruft
*~
.DS_Store

# Generated transducer files (FST build/test outputs).
# Committed test fixtures are added explicitly with `git add -f`.
*.hfst
*.hfstol
*.genhfstol
*.foma

# OpenFST reference clone (behavioural reference for rustfst gap-fills; not committed)
/openfst

# Port scaffolding and local toolchain staging
.port-staging/
/dist/
