//! `hfst-c` — the C API (`libhfst_c`) over the `hfst` facade, exposed via cbindgen.
//!
//! Wave 2 target: `libhfst/src/c/libhfst_c.{cpp,h}` — a 1:1 port of the thin C
//! wrapper (opaque pointers, lookup, stream I/O).
