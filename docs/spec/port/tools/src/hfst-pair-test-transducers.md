# tools/src/hfst-pair-test-transducers.h

> [spec:hfst:def:hfst-pair-test-transducers.hfst.construct-pair-vector-fn]
> KeyPairVector * construct_pair_vector(vector<KeyVector*> &input_fragments)

> [spec:hfst:sem:hfst-pair-test-transducers.hfst.construct-pair-vector-fn]
> TODO(sem): what this does, step by step — precisely enough to
> re-implement from this rule alone, without reading the source.

> [spec:hfst:def:hfst-pair-test-transducers.hfst.delete-key-pair-vector-fn]
> void delete_key_pair_vector(KeyPairVector * v)

> [spec:hfst:sem:hfst-pair-test-transducers.hfst.delete-key-pair-vector-fn]
> TODO(sem): what this does, step by step — precisely enough to
> re-implement from this rule alone, without reading the source.

> [spec:hfst:def:hfst-pair-test-transducers.hfst.display-pair-vector-fn]
> void display_pair_vector(KeyPairVector * v)

> [spec:hfst:sem:hfst-pair-test-transducers.hfst.display-pair-vector-fn]
> TODO(sem): what this does, step by step — precisely enough to
> re-implement from this rule alone, without reading the source.

> [spec:hfst:def:hfst-pair-test-transducers.hfst.display-rule-keys-fn]
> void display_rule_keys(void)

> [spec:hfst:sem:hfst-pair-test-transducers.hfst.display-rule-keys-fn]
> TODO(sem): what this does, step by step — precisely enough to
> re-implement from this rule alone, without reading the source.

> [spec:hfst:def:hfst-pair-test-transducers.hfst.duplicate-keys-fn]
> KeyPairVector * duplicate_keys(KeyVector * v)

> [spec:hfst:sem:hfst-pair-test-transducers.hfst.duplicate-keys-fn]
> TODO(sem): what this does, step by step — precisely enough to
> re-implement from this rule alone, without reading the source.

> [spec:hfst:def:hfst-pair-test-transducers.hfst.fst-vector]
> typedef vector<TransducerHandle> FstVector

> [spec:hfst:def:hfst-pair-test-transducers.hfst.get-alphabet-fn]
> void get_alphabet(char * symbol_file_name)

> [spec:hfst:sem:hfst-pair-test-transducers.hfst.get-alphabet-fn]
> TODO(sem): what this does, step by step — precisely enough to
> re-implement from this rule alone, without reading the source.

> [spec:hfst:def:hfst-pair-test-transducers.hfst.get-symbol-number-fn]
> int get_symbol_number(char * symbol_name)

> [spec:hfst:sem:hfst-pair-test-transducers.hfst.get-symbol-number-fn]
> TODO(sem): what this does, step by step — precisely enough to
> re-implement from this rule alone, without reading the source.

> [spec:hfst:def:hfst-pair-test-transducers.hfst.get-symbol-number-key-fn]
> int get_symbol_number_key(Symbol symbol_number, char * symbol_name)

> [spec:hfst:sem:hfst-pair-test-transducers.hfst.get-symbol-number-key-fn]
> TODO(sem): what this does, step by step — precisely enough to
> re-implement from this rule alone, without reading the source.

> [spec:hfst:def:hfst-pair-test-transducers.hfst.key-pair-vector]
> typedef vector<KeyPair*> KeyPairVector

> [spec:hfst:def:hfst-pair-test-transducers.hfst.key-vector]
> typedef vector<Key> KeyVector

> [spec:hfst:def:hfst-pair-test-transducers.hfst.make-tokenizer-fn]
> void make_tokenizer(void)

> [spec:hfst:sem:hfst-pair-test-transducers.hfst.make-tokenizer-fn]
> TODO(sem): what this does, step by step — precisely enough to
> re-implement from this rule alone, without reading the source.

> [spec:hfst:def:hfst-pair-test-transducers.hfst.read-rule-transducers-fn]
> FstVector * read_rule_transducers(void)

> [spec:hfst:sem:hfst-pair-test-transducers.hfst.read-rule-transducers-fn]
> TODO(sem): what this does, step by step — precisely enough to
> re-implement from this rule alone, without reading the source.

> [spec:hfst:def:hfst-pair-test-transducers.hfst.rule-name-vector]
> typedef vector<char*> RuleNameVector

> [spec:hfst:def:hfst-pair-test-transducers.hfst.split-at-spaces-fn]
> KeyPairVector * split_at_spaces(char * input_string)

> [spec:hfst:sem:hfst-pair-test-transducers.hfst.split-at-spaces-fn]
> TODO(sem): what this does, step by step — precisely enough to
> re-implement from this rule alone, without reading the source.

> [spec:hfst:def:hfst-pair-test-transducers.hfst.split-symbol-pair-fn]
> KeyPair * split_symbol_pair(char * symbol_pair_string)

> [spec:hfst:sem:hfst-pair-test-transducers.hfst.split-symbol-pair-fn]
> TODO(sem): what this does, step by step — precisely enough to
> re-implement from this rule alone, without reading the source.

> [spec:hfst:def:hfst-pair-test-transducers.hfst.symbol-name-pair-vector]
> typedef vector<char*> SymbolNamePairVector

> [spec:hfst:def:hfst-pair-test-transducers.hfst.test-correspondence-fn]
> bool test_correspondence(KeyPairVector * input_vector,

> [spec:hfst:sem:hfst-pair-test-transducers.hfst.test-correspondence-fn]
> TODO(sem): what this does, step by step — precisely enough to
> re-implement from this rule alone, without reading the source.

> [spec:hfst:def:hfst-pair-test-transducers.hfst.tokenize-fn]
> KeyPairVector * tokenize(char * input_string)

> [spec:hfst:sem:hfst-pair-test-transducers.hfst.tokenize-fn]
> TODO(sem): what this does, step by step — precisely enough to
> re-implement from this rule alone, without reading the source.

